# Skill 开发前，业务和接口资料要准备到什么程度

把现有业务接口编排成可以直接使用的 Skill，看起来只是把 MCP 工具按顺序串起来。

实际开发中，时间往往花在反复确认业务规则和字段关系上。

比如，PR 信息查询接口的某个列表字段，需要填写 WBS 状态查询返回的 `taskNumber`；后续接口需要的某个 `id`，实际来自更早的 PO 查询。PR 提交接口返回成功后，流程还没有结束，必须再次调用 PR 信息查询接口取得 `prNo`，才能向用户报告成功。

这些问题不能由 Skill 开发人员根据字段名判断。技术人员可以完成流程编排，也了解大模型的能力边界，但业务含义、接口契约和用户交互仍需由对应负责人确认。

本文整理了进入 Skill 开发前需要准备的交接材料。可直接填写的版本放在 [Skill需求交接模板.md](Skill需求交接模板.md) 中。

## 一、为什么不能拿到接口就直接写

接口说明的是系统提供了什么能力，Skill 还要知道这项能力在具体场景中怎么使用。

开发前至少需要确认下面四件事：

| 需要明确的内容 | 具体要解决的问题 |
|---|---|
| 业务流程 | 什么情况下开始，先做什么，后做什么，在哪里结束 |
| 接口契约 | 调哪个 MCP 工具，字段是什么意思，怎样判断成功 |
| 字段关系 | 每个参数从哪里取得，要不要转换，交给下一个接口的哪个字段 |
| 用户交互 | 什么时候需要用户输入或确认，最后给用户看什么 |

少一项，开发就会停下来等待确认。如果开发过程中一直在逐个核对字段，说明交接材料还没有准备到可以开发的程度。

## 二、大模型、MCP 和 Skill 的分工

### 大模型理解语言，但不了解公司的内部约定

大模型可以理解用户意图、整理上下文、选择工具并生成调用参数。它很适合根据明确规则处理流程。

但它不会天然知道 PR 信息查询请求中某个列表里的数字就是 WBS 查询返回的 `taskNumber`，也不会知道一个名称普通的 `id` 应该取自 PO 查询。字段来源、业务状态、权限范围、可重试错误和展示口径，都需要外部规则说明。

如果资料中没有写，大模型只能结合字段名称和上下文推测。推测可以用来辅助分析，不能直接用于 PR 下发、审批、删除、发送消息这类真实业务操作。

### MCP 给 Agent 提供工具

MCP Server 把现有接口包装成 Agent 可以调用的工具。Agent 在运行时获取工具名称、用途和参数结构，再由模型选择工具、准备参数，最后通过 MCP Server 调用业务系统。

```text
用户
  ↓
Agent / 大模型
  ↓ 选择工具、准备参数
MCP Server
  ↓ 调用现有接口
业务系统
```

这里并不是把所有接口代码和业务数据放进大模型。模型拿到的主要是工具说明、参数结构和调用结果。

因此，接口都注册进 MCP Server 只是第一步。如果工具说明里缺少字段含义、类型、单位、枚举、错误码和示例，模型依然无法稳定调用。

### Skill 负责具体场景的执行方法

Skill 是一份给 Agent 使用的工作说明。它需要写清楚：什么请求会触发这个能力，按什么顺序调用工具，参数如何准备，不同结果走哪个分支，什么时候和用户交互。

MCP 解决的是工具如何提供给 Agent，Skill 解决的是某个业务场景如何使用这些工具。

## 三、传统 IT 流程通了，为什么不代表 Skill 就能用

传统 IT 项目里，用户从固定页面进入，填写固定表单，点击固定按钮。程序已经提前写好了调用哪个接口、字段怎么转换、成功后跳哪个页面。用户能输入什么、系统下一步做什么，大部分都被页面和代码限制住了。

在这种模式下，接口联通、字段传递正确、固定流程跑通，通常已经完成了主要验证。

Skill 面对的入口是自然语言。用户可能说“退一下刚才那单”，也可能只说“这个不想要了”。系统需要先判断用户想做什么、信息是否完整、应该使用哪个 Skill、调用哪个工具，必要时还要向用户追问。工具返回结果后，还要判断业务状态并组织成用户能理解的回复。

两类流程的差别可以直接放在一起看：

| 环节 | 传统 IT 流程 | AI Skill 流程 |
|---|---|---|
| 用户入口 | 固定页面、菜单和按钮 | 自然语言，表达方式不固定 |
| 输入控制 | 表单提前限制字段、格式和必填项 | 先从对话中提取，缺失时需要追问 |
| 流程选择 | 程序代码提前写死 | 模型根据 Skill、上下文和规则判断 |
| 工具选择 | 后端代码直接指定接口 | Agent 根据工具说明选择 MCP 工具 |
| 字段传递 | 映射关系写在程序代码中 | Skill 和字段映射资料必须给出明确依据 |
| 异常处理 | 固定错误页或固定分支 | 需要区分重试、补充信息、确认、终止和转人工 |
| 结果展示 | 页面模板提前写好 | 需要根据业务状态生成准确回复，并控制展示字段 |
| 流程通过的含义 | 固定路径可以正常运行 | 除了能调用，还要证明模型能选对、传对、判断对、说对 |

以 PR 通为例，传统页面已经把 PO、WBS 和用户选中的表格行保存在页面状态中。用户点击“下发”时，程序可以直接读取固定字段并调用后端。Skill 场景中，用户先提出筛选需求，再从 Agent 展示的结果中指定某条 WBS。哪条记录可以单独下发、向用户展示哪些字段、用户选中的行如何变成后续接口参数，都需要另外定义。接口能够执行查询和提交，但不会提供完整的对话流程。

AI 没有替代原来的 IT 系统。它位于用户和现有系统之间，增加了一层自然语言理解和动态决策。

```text
用户表达
  ↓
大模型：理解意图、提取信息
  ↓
Skill：规定流程、判断和交互
  ↓
MCP Server：提供可调用工具
  ↓
现有接口：执行实际业务操作
```

接口通了，只能说明最下面的执行能力可用。MCP 工具能调用，说明 Agent 已经拿到了这个执行能力。Skill 是否可用，还要看上面的意图、规则、字段、分支和交互是否完整。

这也是 AI 项目中常见的误解：把传统接口编排的验收方式直接套到 Skill 上，认为接口 A 调完接口 B，返回结果正常，工作就结束了。这个验证只能证明链路能跑，不能证明 Agent 面对真实用户表达时会做出正确选择。

AI 场景对业务和接口资料的要求不会更低。部分原来藏在页面限制和后端代码里的信息，现在需要明确写进 Skill 或配套资料。否则，同一条技术链路虽然能运行，换一种用户说法、少一个参数或遇到一个异常状态，就可能走错流程。

### 即使只看正常成功流程，也还有很多内容需要确定

暂时不考虑超时、接口报错、校验失败等负向场景，PR 通的正常流程仍然不能只用一张接口顺序图来描述。

| 正常流程中的问题 | 传统 IT 中通常藏在哪里 | Skill 开发需要拿到什么 |
|---|---|---|
| 什么请求进入 PR 通 | 菜单入口和页面路由已经固定 | 触发条件、相似场景边界、用户表达示例 |
| 第一次查询用什么条件 | 页面表单已经限定字段 | 从用户输入和登录上下文中提取哪些值 |
| PO、NPNW、WBS 怎么执行 | 后端代码已经写好顺序 | 顺序、依赖关系，以及是否可以并行 |
| 哪些 WBS 可以单独下发 | 页面或后端已有筛选逻辑 | 完整筛选规则和所需配置字段 |
| 候选 WBS 怎么给用户看 | 表格列和显示格式已经固定 | 展示字段、排序方式、选择方式 |
| 用户选择对应哪条数据 | 页面组件保存了选中行对象 | 用户选择与 `taskNumber` 等真实字段的绑定关系 |
| 后续接口参数从哪里来 | 前后端代码里已有赋值逻辑 | PO `id`、WBS `taskNumber` 等字段的精确来源和目标路径 |
| 自动补齐以什么为准 | 程序已有覆盖和默认值逻辑 | 字段优先级、覆盖规则、默认值和保留规则 |
| 校验通过后是否直接提交 | 按钮和代码控制提交时机 | 自动提交还是用户确认，以及确认时展示什么 |
| 提交后怎样拿到 PR 编号 | 页面已有刷新或轮询逻辑 | 第二次查询的条件、时机和 `prNo` 路径 |
| 什么叫整个场景成功 | 成功页和跳转条件已经写死 | 必须取得 `prNo`，以及最终展示格式 |

这些都属于正向流程，不是异常处理。接口 A、B、C 能依次返回成功，只证明调用链可运行；用户能否正确进入场景、选中正确 WBS、把正确字段传到后续接口，并最终看到正确的 `prNo`，才是 Skill 正常流程需要证明的内容。

## 四、这些内容分别由谁确认

| 内容 | 业务/产品 | 接口文档与 MCP | Skill 开发 |
|---|---|---|---|
| 场景和规则 | 说明适用范围、判断条件和业务结果 | 确认现有工具能否支持 | 检查可实现性并完成编排 |
| 流程和分支 | 确认执行顺序、自动步骤和人工节点 | 对齐每一步对应的工具 | 实现步骤跳转和状态控制 |
| 字段含义 | 确认业务上是否为同一个对象 | 提供字段路径、类型、单位和映射关系 | 按映射取值、转换和校验 |
| 成功和失败 | 定义业务上的成功、失败和部分成功 | 提供业务码、错误含义和调用限制 | 实现重试、终止和兜底 |
| 用户交互 | 确认询问、选择、确认和展示内容 | 说明哪些字段可以返回和展示 | 按确定的交互实现 |
| 工程质量 | 提供权限和合规要求 | 支持鉴权、幂等、限流等能力 | 负责超时、日志、测试和发布 |

Skill 开发人员负责检查方案是否可行，并指出冲突和风险。业务规则、字段语义或用户口径不明确时，应由对应负责人给出最终答案，不能将开发人员的个人理解写进 Skill。

## 五、一句业务描述为什么还不够

例如，PR 通场景只给出下面这段流程：

> 用户进入场景后，筛选可以单独下发 PR 的 WBS。系统调用 PO 查询、NPNW 配置和 WBS 状态查询；用户选择一条 WBS 后，再调用 PR 信息查询、自动补齐、校验和提交接口，最后查询并展示 PR 号。

这句话仍缺少以下信息：

| 需求里没有说明的内容 | 开发前需要确认的问题 |
|---|---|
| 筛选规则 | “可以单独下发 PR”由哪些状态和配置共同决定？PO、NPNW、WBS 三个查询的结果如何组合？ |
| 调用关系 | PO 查询、NPNW 配置和 WBS 状态查询必须按顺序执行，还是可以并行？其中一个失败后是否继续？ |
| WBS 选择 | 候选 WBS 向用户展示哪些字段？用户用序号、名称还是 `taskNumber` 选择？ |
| 跨接口字段 | PR 信息查询某个列表字段为什么要使用 WBS 返回的 `taskNumber`？目标字段的完整路径是什么？ |
| PO 数据来源 | 后续请求中的 `id` 取自 PO 查询的哪个对象？有多个 PO 时如何匹配当前 WBS？ |
| 自动补齐 | 哪些字段由接口补齐，哪些来自前序步骤，已有值冲突时以谁为准？ |
| 校验处理 | PR 校验返回多条问题时，哪些可以自动修复，哪些需要用户补充，如何展示？ |
| 提交结果 | PR 提交成功后为什么还要再次查询？用什么条件查询，多久能查到 `prNo`？ |
| 最终成功 | 是提交接口成功就算完成，还是必须取得非空 `prNo` 才算完成？ |
| 用户展示 | 筛选、选择、校验失败、提交失败和最终成功分别展示什么？ |

这段流程说明了大致顺序，但还不能直接转成 Skill。技术可以把缺口列出来，筛选规则和用户交互由业务确认，字段路径和接口契约由接口负责人补齐。

## 六、交接材料中的四张核心表

背景材料不必很长。下面四张表填清楚，通常已经足够开始编排。

### 1. 场景流程表

这张表用来确定整个场景怎么运行。两个步骤需要自动连续执行，也直接写在表里。

| 步骤 | 做什么 | 什么时候执行 | MCP 工具 | 必填输入 | 输入来源 | 必需输出 | 成功后 | 失败后 | 用户交互 |
|---|---|---|---|---|---|---|---|---|---|
| S01 |  | 用户触发/上一步后自动/满足条件自动/用户确认后 |  |  |  |  |  |  | 无/输入/选择/确认/展示 |

输入来源需要写到具体字段。例如：

```text
USER.pr_filter
CONTEXT.current_user.id
STEP.S01.output.<po_path>.id
STEP.S03.output.<wbs_list_path>[selected].taskNumber
STEP.S04.output.<pr_info_path>
```

“从上一接口获取”对开发没有帮助。PO、NPNW、WBS 和 PR 信息查询的响应中都可能有 `id`、`number` 或列表对象，必须写出具体步骤和字段路径。

### 2. 字段映射表

这张表对复杂接口最重要。字段名称相同，不一定表示同一个值；字段名称不同，也可能指向同一个业务对象。映射关系需要由业务和接口负责人确认。

| 步骤 | 业务含义 | 来源字段或表达式 | 目标字段/使用位置 | 必填条件 | 类型和单位 | 转换规则 | 缺失时处理 | 示例 |
|---|---|---|---|---|---|---|---|---|
| S04 | PO 标识 | `S01.output.<po_path>.id` | `<PR信息查询请求中的目标id字段>` | 按接口规则 | 待接口方说明 | 待接口方说明 | 无法继续查询 | 待补充 |
| S04 | WBS 任务编号 | `S03.output.<wbs_list_path>[selected].taskNumber` | `<PR信息查询请求.list[].目标数字字段>` | 用户选定 WBS 后 | 待接口方说明 | 待接口方说明 | 无法继续查询 | 待补充 |
| S08 | PR 编号 | `S08.output.<pr_result_path>.prNo` | 最终用户展示 | 最终成功必需 | string | 去除空值后展示 | 不得报告成功 | `PRxxxxxxxx` |

表中的尖括号表示实际路径尚未提供，必须由接口负责人补齐，不能在开发阶段根据示例数据猜测。

PR 通场景中的部分接口有四十多个出入参，部分响应超过一百个字段。这类接口需要同时提供完整 Schema、字段说明和成功/失败示例。字段说明至少要覆盖类型、业务含义、是否必填、条件必填规则、枚举、默认值、数组子结构、空值含义和敏感级别。

完整接口文档负责解释全部字段；Skill 交接表再从中标出本场景实际使用的字段及其来源、去向。只有一个超长 JSON 示例，没有字段说明，无法承担这两项作用。

### 3. 结果处理表

单个接口调用成功不代表 PR 通场景已经完成。最终是否成功，需要结合校验、提交和第二次 PR 信息查询的结果判断。

| 情况 | 判断方式 | 系统处理 | 用户看到的内容 | 后续动作 |
|---|---|---|---|---|
| 没有可单独下发的 WBS | 筛选结果为空 | 不进入 PR 信息查询 | 当前没有可单独下发 PR 的 WBS | 结束或调整筛选条件 |
| WBS 候选不唯一 | 返回多条可下发记录 | 等待用户选择，不自动取第一条 | 展示可区分的 WBS 信息 | 用户指定一条 |
| PR 信息校验失败 | 校验接口返回问题列表 | 按问题类型分流 | 展示业务认可的问题说明 | 补充、修改或结束 |
| PR 提交状态未知 | 提交接口超时或结果不明确 | 先查询提交结果，不直接重复提交 | 暂时无法确认 PR 提交结果 | 查询或转人工 |
| 提交成功但没有 `prNo` | 第二次 PR 信息查询未返回非空 `prNo` | 按约定轮询或进入异常处理 | PR 已提交，但暂未取得 PR 编号 | 查询或转人工 |
| 取得 `prNo` | 第二次 PR 信息查询返回非空 `prNo` | 保存并展示 `prNo` | PR 下发成功，PR 编号为 `{prNo}` | 流程结束 |

### 4. 用户交互表

用户交互会直接改变流程。例如，是否需要确认，决定了写接口能不能立即调用。这部分需要在开发前确定。

| 所在步骤 | 触发条件 | 交互方式 | 展示内容或文案 | 用户操作 | 取消后处理 |
|---|---|---|---|---|---|
| WBS 筛选完成 | 返回一条或多条可单独下发记录 | 列表选择 | 展示业务确认的 WBS 识别字段 | 选择一条、取消 | 结束，不进入 PR 信息处理 |
| PR 校验失败 | 校验接口返回可修正问题 | 补充或选择 | 展示字段名称、问题原因和允许的处理方式 | 补充、修改、取消 | 结束或重新校验 |
| 最终查询取得 `prNo` | `prNo` 非空且状态满足成功条件 | 结果展示 | PR 下发成功，PR 编号为 `{prNo}` | 复制编号、结束 | 流程结束 |

缺少信息时问哪些字段，查到多个对象时展示什么，哪些操作必须确认，失败后允许重试还是转人工，这些都属于业务交互，需要由业务或产品确认。技术负责检查所需字段能否取得，并按确认后的方案实现。

## 七、以 PR 通场景为例

PR 通的目标是筛选可以单独下发 PR 的 WBS，由用户指定一条 WBS 后完成 PR 信息准备、校验和提交，最终把 `prNo` 展示给用户。

按目前的业务描述，主流程如下：

| 步骤 | 动作 | MCP 工具 | 执行方式 | 关键输入来源 | 关键输出或下一步 |
|---|---|---|---|---|---|
| S00 | 接收 PR 筛选条件 | 无 | 用户进入场景后 | 用户输入和登录上下文 | 自动进入 S01 |
| S01 | 查询 PO | PO 查询接口 | 自动 | S00 筛选条件，具体字段待确认 | 输出后续需要的 PO 对象和 `id` |
| S02 | 查询 NPNW 配置 | NPNW 配置接口 | 自动，和 S01 的依赖关系待确认 | 具体来源待确认 | 输出筛选或补齐所需配置 |
| S03 | 查询 WBS 状态 | WBS 状态查询接口 | 自动，和前两步的依赖关系待确认 | PO、NPNW 或用户条件，具体映射待确认 | 得到可参与筛选的 WBS 及 `taskNumber` |
| UI01 | 用户选择 WBS | 无 | 筛选出可单独下发项后 | S03 候选列表 | 用户指定一条 WBS，进入 S04 |
| S04 | 第一次查询 PR 信息 | PR 信息查询接口 | 用户选择后自动 | PO `id`、所选 WBS 的 `taskNumber` 等 | 得到 PR 基础信息 |
| S05 | 自动补齐 PR 信息 | PR 信息自动补齐接口 | S04 成功后自动 | S04 结果、PO/NPNW/WBS 数据 | 得到补齐后的 PR 数据 |
| S06 | 校验 PR 信息 | PR 信息校验接口 | S05 成功后自动 | 补齐后的完整 PR 数据 | 通过则进入 S07；失败按问题类型处理 |
| S07 | 提交 PR | PR 提交接口 | 校验通过后 | 已校验 PR 数据、幂等信息等 | 提交成功后进入 S08 |
| S08 | 第二次查询 PR 信息 | PR 信息查询接口 | S07 成功后自动 | 提交结果对应的查询条件 | 取得非空 `prNo` 后展示给用户 |

这条流程已经说明了接口顺序，但仍有大量字段级信息没有给出。例如：

| 已知关系 | 仍需接口文档补充的内容 |
|---|---|
| PR 信息查询的某个列表数字来自 WBS 的 `taskNumber` | PR 请求中的完整目标路径、类型、列表构造规则、是否允许多项 |
| 后续某个 `id` 来自 PO 查询 | PO 响应中的完整来源路径、目标接口字段路径、多条 PO 时的匹配规则 |
| NPNW 配置参与后续处理 | 使用哪些配置字段、影响筛选还是自动补齐、冲突时的优先级 |
| PR 自动补齐结果进入校验 | 哪些字段会被覆盖、哪些字段必须保留前序值、空值如何处理 |
| PR 提交后再次查询 | 第二次查询使用什么主键、是否需要等待或轮询、查询不到时如何处理 |
| 最终展示 `prNo` | `prNo` 的准确响应路径，以及与成功状态的联合判断条件 |

这里不能用“相关字段从前序接口获取”代替映射。一次调用有四十多个入参时，每个实际使用的入参都要有来源；一次响应有一百多个字段时，每个被后续判断、调用或展示使用的字段都要有去向。

最终成功也需要明确为一个可检查的条件：

```text
S07 PR 提交满足接口定义的成功条件
并且
S08 PR 信息查询返回满足业务状态要求的非空 prNo
```

只有 S07 返回成功，但 S08 没有取得 `prNo`，不能直接向用户展示“PR 下发成功”。此时是等待查询、重试查询还是转人工，需要业务和接口方提前确定。

对传统 IT 页面来说，这些字段映射、页面状态和展示文案可能已经写在前后端代码中，所以固定流程容易跑通。Skill 需要把原来藏在代码里的关系整理成模型可以理解、程序可以校验的明确资料。业务只提供大致流程，接口只提供没有字段说明的超长报文，再由 Skill 开发人员补全用户输入输出和字段关系，责任边界是不合理的。

## 八、开工前检查什么

| 检查的问题 | 需要确认的答案 |
|---|---|
| 什么时候开始执行？ | 明确的触发条件和业务规则 |
| 每一步调用什么？ | 确定的 MCP 工具和版本 |
| 必填参数从哪里取得？ | 精确到字段路径的来源 |
| 参数是否需要转换？ | 类型、单位、格式和转换规则 |
| 怎样判断结果？ | 成功、失败、处理中和状态未知的定义 |
| 什么时候需要用户参与？ | 输入、选择、确认和取消规则 |
| 最后展示什么？ | 已确认的字段、文案和后续入口 |

这些内容齐全后，Skill 开发会比较直接：按照流程选择工具，按映射准备参数，按结果进入下一个步骤，再补上校验、幂等、日志和测试。

如果其中某项还没有答案，应标出问题并由对应负责人确认，避免把个人判断写成正式业务规则。

验收时也不能只用一句标准话术把接口链路跑通。至少要覆盖同一意图的不同说法、信息缺失、表达有歧义、不应触发该 Skill 的请求，以及接口超时、业务拒绝和状态未知等情况。这样才能验证 Agent 在真实对话中是否会选对工具、传对参数并给出正确说明。
